{
  "name": "finoffice-vercel-backend",
  "version": "1.0.0",
  "private": true,
  "description": "FinOffice com frontend estatico e API serverless para Vercel usando MongoDB Atlas.",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "check": "node --check server.js && node --check api/health.js && node --check api/users/index.js && node --check api/users/[id].js && node --check api/transactions/index.js && node --check api/transactions/[id].js && node --check lib/db.js && node --check lib/env.js && node --check lib/http.js && node --check lib/serializers.js && node --check lib/validation.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "mongodb": "^6.16.0"
  },
  "engines": {
    "node": ">=20"
  }
}
